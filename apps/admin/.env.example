# 로컬 Supabase DB 직접 연결 (Prisma — RLS 우회, 내부 도구 전용)
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"

# Claude API 키 — 없으면 AI 생성 비활성(모의 생성만 가능)
# ANTHROPIC_API_KEY="sk-ant-..."

# 서버 포트 (기본 3100)
# PORT=3100
