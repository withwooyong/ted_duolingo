{
  "name": "@ted/shared",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'shared: lint 없음 (typecheck로 대체)'",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "~5.9.0",
    "vitest": "^4.1.8"
  }
}
